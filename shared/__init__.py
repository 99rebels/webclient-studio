"""Freelance Forge — shared utilities for the skill bundle.

Public modules:
    db_helper      — SQLite CRUD, queries, activity log, export
    web_research   — fetch + extract company info (Playwright optional)
    templates      — minimal {{var}} / {{#section}} renderer
"""
